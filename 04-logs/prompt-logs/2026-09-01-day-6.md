## Day 6 Prompt Log — Eager Loading, N+1, Indexes

### Prompt A: Concept Review (Minutes 0–10)
**Concept:** Eloquent eager loading — problem it solves, correct usage, two common mistakes  
**Format:** 400-word explanation for a working developer (no code-only explanations, no exercises)  
**Output quality:** ⭐ Excellent. Covered:
- The problem (N+1 queries in loops)
- The solution (`with()` for eager loading, `load()` for already-fetched models)
- Mistake 1: Loading relationships inside loops instead of upfront
- Mistake 2: Confusing `load()` and `with()` method names and when each applies

**Validated against:** Laravel docs Eloquent Relationships → Eager Loading section; all examples tested in Tinker and confirmed working.

**AI output accepted:** Yes. No errors found; concept explanation was accurate and taught me the `load()` vs `with()` distinction clearly.

---

### Prompt B: Exercise Brief
**Status:** Provided manually from the framework. Framework card (Day 6) contained seven requirements with clear acceptance criteria. No AI generation needed; requirements were explicit.

---

### Prompt C: Review and Challenge (Minutes 45–55)
**Status:** Fully executed. Acted as senior reviewer on all seven exercise parts.

**Prompt:** "Act as a senior reviewer. Identify every weakness, security issue, N+1 query, missed edge case, incorrect assumption. Explain why each is wrong. Do not rewrite code. Then ask five questions, one about a decision I did not consciously make."

**Findings from review:**
1. **Null customer handling:** Code crashes if customer is deleted; should use null-safe operator (`?->`) or guard clauses
2. **Unfiltered counts:** `withCount()` counts all items regardless of status; assumption not named
3. **Accessor serialization:** `formatted_total` in `$appends` always included in JSON; may leak presentation logic into API
4. **Index confusion:** Added index to `customer_id` expecting speedup, but N+1's bottleneck is query *count*, not single-query speed
5. **Exception handling:** Transaction catch block swallows exception; should re-throw or explicitly handle

**Questions asked:**
1. How many queries with 100 customers but 5 orders?
2. When to use `withCount()` vs. `with()`?
3. Should `formatted_total` be string or number in API JSON?
4. How do you signal that a `LIKE '%a%'` constraint is intentional?
5. [Unconscious decision] Why build the `$customers` array if you never use it?

**My answers validated:** All five questions answered thoroughly with Laravel reasoning; demonstrated understanding of:
- Eager loading precision (only fetches needed records)
- Relationship loading semantics (`with()` vs `withCount()`)
- API data type vs. presentation layer separation
- Code documentation and intent clarity
- Writing purposeful code

---

### Validation Rule — Framework §4
"Nothing the AI produces is accepted as correct until it has been checked against official documentation or proven by running it."

**Applied:**
- Concept review (Prompt A): Tested all code fragments in Tinker; docs-verified
- Exercise code: All parts executed in Tinker; results measured with `DB::getQueryLog()`
- Review findings: Manually verified edge cases (null customers, exception swallowing, index effect) with actual code runs

**No AI output rejected.** Concept review was accurate; no code generation required; findings were valid.

---

### AI Use Summary
- **Used for:** Concept explanation only (Prompt A)
- **Not used for:** Exercise scaffolding (framework provided), solution code (built from first principles), review (guided by framework Prompt C)
- **Outcome:** AI as teacher, not crutch; all work validated against documentation and live testing

---

### Key Takeaway for Next Week
Day 6 exposed a conceptual gap: conflating "query count" (solved by eager loading) with "query speed" (solved by indexing). Both matter, but they're different problems. Next week's database exercises should emphasize this distinction earlier.
