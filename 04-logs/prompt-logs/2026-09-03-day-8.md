# Prompt Log — Day 8 — Authentication and Authorisation (3 September 2026)

## Prompt A — Concept Review (0–10 min)

**Prompt asked:**
> Explain API authentication and authorisation to a working developer in under 600 words. Cover what problem each solves, the correct usage, and the two mistakes developers most commonly make with each. No exercises, no code beyond short illustrative fragments.

**Output quality:** ✅ Excellent. Clearly separated authentication ("who are you") from authorisation ("what can you do"), explained Sanctum + Policies, covered common mistakes (logging tokens, trusting unverified JWTs, returning 404 instead of 403, scattering permission checks).

**How validated:**
- Checked against Laravel Sanctum documentation ✓
- Verified 401 vs 403 distinction against HTTP spec ✓
- Checked against OWASP guidelines for token handling ✓
- Used immediately to design the day's implementation with confidence ✓

**Outcome:** Prevented conflating authentication and authorisation in code design.

---

## Prompt C — Review and Challenge (45–55 min)

**Prompt asked:**
> Here is my Exercise 8 solution (AuthController, OrderPolicy, OrderController, routes). Act as a senior reviewer. Identify every weakness, security issue, N+1 query, missed edge case and incorrect assumption. Explain **why** each one is wrong. **Do not rewrite my code.** Then ask me five questions about it, one of which should be about a decision I did not consciously make.

**Output quality:** ✅ Strong. Identified 5 findings:
1. **Gap:** Exception handler for authorization failures may not use error envelope (flagged to verify)
2. **Gap:** Rate limiting not tested (flagged to test manually)
3. **Gap:** Multiple-login behavior untested (flagged as design decision to document)
4. **Gap:** Deleted-user edge case untested (flagged to verify returns 401, not 500)
5. **Correct:** Cross-user access correctly returns 403 with no data leak ✓

**Five questions:**
1. Where could tokens leak into logs? (caught potential in exception handler)
2. Does Policy denial return 403 as JSON? (prompted verification)
3. What happens on 6th rate-limit attempt? (prompted testing)
4. Does 403 leak resource existence? (prompted testing)
5. Multiple logins create multiple tokens? (identified unconscious design decision)

**How validated:**
- Tested rate limiting manually: 429 on 6th attempt ✓
- Tested multiple-token scenario: both tokens active ✓
- Tested deleted-user scenario: 401 returned (not 500) ✓
- Tested cross-user access: 403 returned (not 404) ✓
- Checked exception handler code (working correctly) ✓

**AI findings that turned out to be wrong:** None. All five flagged items were either confirmed or revealed genuine design decisions needing documentation.

**AI findings that were validated against actual requests:** All five tested in Postman and confirmed.

---

## Summary for Week 2 progress
- Prompt A saved 15+ minutes of docs reading and clarified the core distinction upfront
- Prompt C's systematic review exposed the one real gap (exception handler) and identified three design decisions needing documentation
- All attacks suggested by AI were tested and either worked (proving the concern real) or didn't (proving the defense sound)
- Ready to move to Week 3 (mini-app backend) with confidence in the authentication and authorisation patterns
