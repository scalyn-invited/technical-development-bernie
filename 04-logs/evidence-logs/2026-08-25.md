---
date: 2026-08-25
day: 2
topic: JavaScript — promises, async/await, fetch and error handling
---

# Day 2 Evidence Log — Tuesday 25 August 2026

## Exercise Completed

**Exercise 2:** Build a single-file Node.js script that fetches from a public API (JSONPlaceholder) with comprehensive error handling.

## Acceptance Criteria — Status

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Handles loading state | ✅ | Console logs "Fetching data..." and "Fetching multiple data..." |
| Handles network failure | ✅ | AbortError caught when timeout triggers; generic network errors caught |
| Handles non-2xx response distinctly | ✅ | Checks `response.ok` and throws HTTPError with status code |
| Handles valid-but-empty result set | ✅ | Checks `data.length === 0` and throws error |
| Fetches three resources concurrently | ✅ | Uses `Promise.allSettled` with three JSONPlaceholder endpoints |
| Reports which one failed | ✅ | forEach loop logs `Request ${index + 1} succeeded/failed` |
| No unhandled rejections | ✅ | All errors caught in try/catch; errors re-thrown for allSettled |
| AbortController with 5-sec timeout | ✅ | `setTimeout` aborts controller after 5000ms; signal passed to fetch |

## Code Built

**File:** `week-1-foundations/day-2-exercise.js`

```javascript
async function fetchData(apiUrl) {
    const controller = new AbortController();
    const timeout = setTimeout(() => {
        controller.abort();
    }, 5000);

    console.log("Fetching data...");
    try {
        const response = await fetch(apiUrl, {
            signal: controller.signal
        });
        if(!response.ok) throw new Error(`HTTPError: ${response.status}`);
        const data = await response.json();
        if (data.length === 0) {
            throw new Error("Empty response");
        }
        console.log(data);
    } catch (error) {
        if (error.name === "AbortError") {
            console.error("Request timed out.");
        } else if (error.message.startsWith("HTTPError")) {
            console.error(error.message);
        } else {
            console.error("Network error: Could not reach the server.");
        }
        throw error;
    } finally {
        clearTimeout(timeout);
    }
}

async function fetchMultipleData(apiUrls) {
    console.log("Fetching multiple data...");
    const promises = apiUrls.map(fetchData);
    const results = await Promise.allSettled(promises);

    results.forEach((result, index) => {
        if (result.status === "fulfilled") {
            console.log(`Request ${index + 1} succeeded:`, result.value);
        } else {
            console.error(`Request ${index + 1} failed:`, result.reason);
        }
    });

    return results;
}

fetchMultipleData([
    "https://jsonplaceholder.typicode.com/posts/1",
    "https://jsonplaceholder.typicode.com/users/1",
    "https://jsonplaceholder.typicode.com/comments/154"
]);
```

## Test Results

**Run command:**
```bash
node week-1-foundations/day-2-exercise.js
```

**Expected output:**
- Loads three resources from JSONPlaceholder
- Logs which requests succeeded
- Demonstrates concurrent fetching with Promise.allSettled
- Reports both fulfilled and rejected outcomes

## Bugs Identified During Review

| Bug | Severity | Status |
|-----|----------|--------|
| String comparison typo (HTTPError vs http error) | Medium | Known, not yet fixed |
| Empty check fails for single objects (e.g., `/posts/1`) | Medium | Known, not yet fixed |
| Malformed JSON not distinctly handled | Low | Known, not yet fixed |

**Notes:** These were identified in the AI review block (45–55). Fixing them is a task for revision or Day 3 debugging practice, not a blocker for acceptance.

## Learning Validation

**Key concepts validated:**
- ✅ fetch resolves on any HTTP status; only network failures cause rejection
- ✅ Promise.allSettled catches both fulfilled and rejected promises without throwing
- ✅ AbortController can terminate a fetch mid-flight
- ✅ Structured error handling distinguishes timeout, HTTP error, and network error

**Remaining gaps** (from development log):
- Exact boundary between fetch rejection and caught errors is still fuzzy
- Single-object vs. array response handling not yet clear

## Commit Hash

```
(Pending — to be committed with conventional message)
```

**Conventional message:**
```
feat: day 2 exercise - async/await and fetch error handling

- Fetch three JSONPlaceholder resources concurrently
- Distinct handling for network, HTTP, and abort errors  
- Promise.allSettled to report individual failures
- AbortController with 5-second timeout
```

## AI Assistance Log

**Prompts used:**
- Prompt A (Concept Review): Explained promises, async/await, fetch; clarified fetch rejection vs. HTTP errors
- Prompt C (Review & Challenge): Senior-level review identifying edge cases and unhandled scenarios

**AI output validated:** Yes — explanations checked against MDN documentation; all claims about fetch behavior confirmed.

**AI output rejected:** None — exercise was built unaided; AI used only for concept review and validation.

---

**Session complete.** Ready for Day 3 (Wednesday 26 August) — Laravel routing, controllers, and a debugging exercise.
