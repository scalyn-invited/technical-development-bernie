## Day 5 — Friday 28 August — Eloquent relationships and validation

Built:            Four relationships (hasMany/belongsTo, belongsToMany with pivot carrying position+status, hasManyThrough) + Form Request with required/exists/unique/custom validation rules on Player, Team, MatchGame, Venue models.

Learned:          Tinker requires full namespace (App\Models\Player::count()) for models; pivot data accessed via ->pivot->column syntax; hasManyThrough flattens a multi-level query in one place.

Got wrong:        Tried to call Player::count() without namespace in tinker, assumed model auto-loaded by name alone.

AI output rejected: None — relationship examples worked directly.

Still cannot explain: When hasManyThrough genuinely saves a query vs when it's just syntax convenience; the performance difference between it and nested eager loading with ->load('matchGames.venues').

Rebuild test result: Day 2 (JavaScript async/await + fetch with error handling) — **PASSED**. Built AbortController with 5s timeout, concurrent fetching with Promise.all, distinct handling for network failure vs HTTP non-2xx, empty result handling. Shows async/promise understanding from Week 1 retained.

Evidence:         Model files with relationships; match_game_player migration; StorePlayerRequest with four validation rule types; rebuild test file week-1-practical-review.js — all buildable and tested in tinker.

Applied to client work: not yet
